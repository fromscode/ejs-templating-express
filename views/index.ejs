<html>
    <title>
        Homepage
    </title>
    <link rel="stylesheet" href="./styles.css">
    <body>
        <%- include('navbar', {links: links}) %>
        <%= message %>
        <ul>
            <% users.forEach((user) => { %>
              <%- include('users/users', {user: user}); %>
            <% }); %>
          </ul>
    </body>
</html>